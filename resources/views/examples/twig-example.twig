<!DOCTYPE html>
<html lang="{{ app.getLocale() }}">
    <head>
        <meta charset="utf-8">
        <meta name="viewport" content="width=device-width, initial-scale=1">
        <title>Twig Example - {{ title }}</title>
        
        {% if app.environment('local') %}
            <script src="http://localhost:3000/{{ asset }}"></script>
        {% else %}
            <link rel="stylesheet" href="{{ asset }}">
        {% endif %}
    </head>
    <body>
        <div class="container">
            <h1>{{ title }}</h1>
            
            <p>This is an example Twig template in Bsidlify.</p>
            
            {% if items %}
                <ul>
                {% for item in items %}
                    <li>{{ item }}</li>
                {% endfor %}
                </ul>
            {% else %}
                <p>No items to display.</p>
            {% endif %}
            
            <p>Current time: {{ "now"|date("Y-m-d H:i:s") }}</p>
            
            {% include "examples/partials/footer.twig" %}
        </div>
    </body>
</html> 